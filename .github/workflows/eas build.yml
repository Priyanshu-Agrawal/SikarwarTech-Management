name: EAS Build
on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
      - name: Setup Node.js
        uses: actions/setup-node@v1
        with:
          node-version: 16.x
      - name: Setup Expo
        uses: expo/expo-github-action@v6
        with:
          expo-version: 4.x
          token: ${{ secrets.EXPO_TOKEN }}
          expo-cache: true
      - name: Install Dependencies
        run: npm ci
      - name: Build on EAS
        run: npx eas-cli build --platform android --non-interactive --local --output=./app-build \
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: false
          prerelease: false
      - name: Upload Release Asset
        id: upload-release-asset 
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./app-build/*.apk
          asset_name: app.apk
          asset_content_type: application/vnd.android.package-archive
      # - name: Upload Release Asset
      #         id: upload-release-asset 
      #         uses: sekwah41/upload-release-assets@v1.1.0
      #         env:
      #           GITHUB_TOKEN: ${{ secrets.PAT }} # Using PAT instead of GITHUB_TOKEN
      #         with:
      #           upload_url: ${{ steps.create_release.outputs.upload_url }}
      #           asset_path: ./app-build/*.apk
      #           asset_name: app.apk
      #           asset_content_type: application/vnd.android.package-archive
